# Nix
result
result-*
.direnv/

# OpenTofu — state never in repo (uses MinIO remote backend)
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
terraform.tfvars

# sops — plaintext secrets never in repo
secrets/*.dec

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
